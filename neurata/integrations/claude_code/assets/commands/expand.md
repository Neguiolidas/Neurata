---
description: Open a Neurata grain at the grain level you need: card, summary or full body.
argument-hint: <grain-id-or-ref> [card|summary|full]
---

Use the `neurata_expand` MCP tool with the reference: $ARGUMENTS

If no grain level is given, start at `summary` and go to `full` only if the
summary is not enough for the task at hand.
